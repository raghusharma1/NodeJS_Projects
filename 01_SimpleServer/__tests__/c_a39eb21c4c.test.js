// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=c_e5ad5cd4a4
ROOST_METHOD_SIG_HASH=c_a39eb21c4c


*/

// ********RoostGPT********
// Importing necessary modules
const bootstrap = require('../js/bootstrap.min');

// Test Suite for c function
describe('c function in bootstrap.min.js', () => {
  let mockElement, mockData;
  
  // Setup before each test
  beforeEach(() => {
    mockElement = {
      each: jest.fn().mockImplementation((callback) => callback()),
      data: jest.fn()
    };
    mockData = {
      "bs.scrollspy": 'testData'
    };
    global.a = jest.fn().mockReturnValue(mockElement);
  });

  // Test Case 1: check if 'each' method is called on the passed object
  test('should call each method on the passed object', () => {
    bootstrap.c('test');
    expect(mockElement.each).toHaveBeenCalled();
  });

  // Test Case 2: check if 'data' method is called with correct parameter
  test('should call data method with "bs.scrollspy"', () => {
    mockElement.data.mockReturnValue(mockData);
    bootstrap.c('test');
    expect(mockElement.data).toHaveBeenCalledWith('bs.scrollspy');
  });

  // Test Case 3: check if 'data' method is called with new data when no data exists
  test('should call data method with new data when no data exists', () => {
    mockElement.data.mockReturnValueOnce(undefined).mockReturnValueOnce('newData');
    bootstrap.c('test');
    expect(mockElement.data).toHaveBeenLastCalledWith('bs.scrollspy', 'newData');
  });

  // Test Case 4: check if 'c' method is called when a string is passed as parameter
  test('should call c method when a string is passed as parameter', () => {
    mockData.c = jest.fn();
    mockElement.data.mockReturnValue(mockData);
    bootstrap.c('test');
    expect(mockData.c).toHaveBeenCalled();
  });
});
