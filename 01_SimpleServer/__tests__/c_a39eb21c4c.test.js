// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=c_e5ad5cd4a4
ROOST_METHOD_SIG_HASH=c_a39eb21c4c


*/

// ********RoostGPT********
// Importing the dependencies
const bootstrap = require('../js/bootstrap.min.js');

// Test suite for function c
describe('Function c Test Suite', () => {
  let a, b, c, d, e, f;

  // Setup before each test case
  beforeEach(() => {
    a = jest.fn();
    b = jest.fn();
    c = jest.fn();
    d = jest.fn();
    e = jest.fn();
    f = jest.fn();
  });

  // Test case 1: Check if function c calls the each function of this object
  test('Check if function c calls the each function of this object', () => {
    const thisObject = { each: jest.fn() };
    bootstrap.c.call(thisObject, c);
    expect(thisObject.each).toHaveBeenCalled();
  });

  // Test case 2: Check if function c calls the correct methods with correct arguments
  test('Check if function c calls the correct methods with correct arguments', () => {
    const thisObject = { each: jest.fn() };
    bootstrap.c.call(thisObject, c);
    expect(a).toHaveBeenCalledWith(thisObject);
    expect(d.data).toHaveBeenCalledWith("bs.scrollspy");
    expect(e.c).toHaveBeenCalled();
  });
});
