// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=f_14aa59bc21
ROOST_METHOD_SIG_HASH=f_daa16fdc9f


*/

// ********RoostGPT********
// Importing necessary modules
const $ = require('jquery');
global.$ = $;
const bootstrap = require('../js/bootstrap.min.js');

describe('Bootstrap Min Function', () => {
  let b, g, e;

  beforeEach(() => {
    // Create some mock jQuery elements
    g = $('<div class="active"><div class="dropdown-menu"><div class="active"></div></div></div>');
    b = $('<div></div>');
    e = jest.fn();
  });

  test('Removes "active" class from g and its children, adds "active" class to b and its parents', () => {
    bootstrap.f.call({ $element: b, $target: g, hoverState: e });

    expect(g.hasClass('active')).toBe(false);
    expect(g.find('.dropdown-menu > .active').hasClass('active')).toBe(false);
    expect(b.hasClass('active')).toBe(true);
    expect(b.parent('.dropdown-menu').closest('.dropdown').hasClass('active')).toBe(true);
  });

  test('Calls the hoverState function if it exists', () => {
    bootstrap.f.call({ $element: b, $target: g, hoverState: e });

    expect(e).toHaveBeenCalled();
  });

  test('Does not call the hoverState function if it does not exist', () => {
    bootstrap.f.call({ $element: b, $target: g, hoverState: null });

    expect(e).not.toHaveBeenCalled();
  });
});
