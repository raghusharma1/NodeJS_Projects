// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=f_14aa59bc21
ROOST_METHOD_SIG_HASH=f_daa16fdc9f


*/

// ********RoostGPT********
// Importing required functions and modules
const { f } = require('../js/bootstrap.min');

// Test suite for function f
describe('Testing f function in bootstrap.min.js', () => {
  let b, g, e, h;

  // Setting up the preconditions before each test
  beforeEach(() => {
    b = {
      addClass: jest.fn(),
      removeClass: jest.fn(),
      find: jest.fn().mockReturnThis(),
      attr: jest.fn(),
      parent: jest.fn(),
      closest: jest.fn(),
      offsetWidth: 0,
    };
    g = { removeClass: jest.fn().mockReturnThis(), find: jest.fn().mockReturnThis() };
    e = jest.fn();
    h = false;
  });

  // Test case 1: Check if the function calls the right methods with the right arguments
  test('Check if the function calls the right methods with the right arguments', () => {
    f();
    expect(g.removeClass).toHaveBeenCalledWith('active');
    expect(g.find).toHaveBeenCalledWith('> .dropdown-menu > .active');
    expect(b.addClass).toHaveBeenCalledWith('active');
    expect(b.removeClass).toHaveBeenCalledWith('fade');
    expect(b.parent).toHaveBeenCalledWith('.dropdown-menu');
    expect(b.closest).toHaveBeenCalledWith('li.dropdown');
    expect(b.attr).toHaveBeenCalledWith('aria-expanded', true);
    expect(e).toHaveBeenCalled();
  });

  // Test case 2: Check if the function handles the 'h' variable correctly
  test('Check if the function handles the \'h\' variable correctly', () => {
    h = true;
    f();
    expect(b.addClass).toHaveBeenCalledWith('in');
  });
});
