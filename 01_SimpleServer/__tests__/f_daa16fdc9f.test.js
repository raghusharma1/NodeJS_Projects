// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=f_14aa59bc21
ROOST_METHOD_SIG_HASH=f_daa16fdc9f


*/

// ********RoostGPT********
// Importing necessary modules
const $ = require('jquery');
global.$ = $;
const bootstrap = require('../js/bootstrap.min.js');

describe('Bootstrap Min Function', () => {
  let b, g, e;

  beforeEach(() => {
    // Create some mock jQuery elements
    b = $(`<div class="dropdown-menu"></div>`);
    g = $(`<div class="dropdown-menu active"></div>`);
    e = jest.fn();

    // Add them to the DOM
    document.body.innerHTML = '';
    document.body.appendChild(b);
    document.body.appendChild(g);
  });

  test('should remove "active" class from g and add to b', () => {
    bootstrap.f();
    expect(g.hasClass('active')).toBe(false);
    expect(b.hasClass('active')).toBe(true);
  });

  test('should call function e', () => {
    bootstrap.f();
    expect(e).toHaveBeenCalled();
  });

  test('should set aria-expanded to true for b', () => {
    bootstrap.f();
    expect(b.attr('aria-expanded')).toBe('true');
  });

  test('should set aria-expanded to false for g', () => {
    bootstrap.f();
    expect(g.attr('aria-expanded')).toBe('false');
  });
});
