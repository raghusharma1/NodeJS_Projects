// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=f_14aa59bc21
ROOST_METHOD_SIG_HASH=f_daa16fdc9f


*/

// ********RoostGPT********
// Import the necessary dependencies
const bootstrap = require('../js/bootstrap.min.js');

// Test suite for the 'f' function
describe('Function f', () => {
    let b, e, g;

    // Set up the necessary preconditions before each test
    beforeEach(() => {
        b = {
            addClass: jest.fn(),
            removeClass: jest.fn(),
            find: jest.fn(),
            attr: jest.fn(),
            parent: jest.fn(),
            closest: jest.fn()
        };

        e = jest.fn();

        g = {
            removeClass: jest.fn(),
            find: jest.fn()
        };
    });

    // Test case 1: Check if the 'removeClass' method of 'g' is called correctly
    test('Check if the removeClass method of g is called correctly', () => {
        bootstrap.f(b, e, g);
        expect(g.removeClass).toHaveBeenCalledWith('active');
    });

    // Test case 2: Check if the 'addClass' method of 'b' is called correctly
    test('Check if the addClass method of b is called correctly', () => {
        bootstrap.f(b, e, g);
        expect(b.addClass).toHaveBeenCalledWith('active');
    });

    // Test case 3: Check if the 'find' method of 'b' is called correctly
    test('Check if the find method of b is called correctly', () => {
        bootstrap.f(b, e, g);
        expect(b.find).toHaveBeenCalledWith('[data-toggle="tab"]');
    });

    // Test case 4: Check if the 'attr' method of 'b' is called correctly
    test('Check if the attr method of b is called correctly', () => {
        bootstrap.f(b, e, g);
        expect(b.attr).toHaveBeenCalledWith('aria-expanded', true);
    });

    // Test case 5: Check if the 'e' function is called correctly
    test('Check if the e function is called correctly', () => {
        bootstrap.f(b, e, g);
        expect(e).toHaveBeenCalled();
    });
});
