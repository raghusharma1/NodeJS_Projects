// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=f_14aa59bc21
ROOST_METHOD_SIG_HASH=f_daa16fdc9f


*/

// ********RoostGPT********
const { f } = require('../js/bootstrap.min.js');

describe('bootstrap.min.js', () => {

  let g, b, e;

  beforeEach(() => {
    g = {
      removeClass: jest.fn().mockReturnThis(),
      find: jest.fn().mockReturnThis(),
      end: jest.fn().mockReturnThis(),
      attr: jest.fn()
    };

    b = {
      addClass: jest.fn().mockReturnThis(),
      removeClass: jest.fn().mockReturnThis(),
      find: jest.fn().mockReturnThis(),
      parent: jest.fn().mockReturnThis(),
      closest: jest.fn().mockReturnThis(),
      attr: jest.fn(),
      offsetWidth: 0
    };

    e = jest.fn();
  });

  test('f function', () => {
    window.g = g;
    window.b = b;
    window.h = true;
    window.e = e;

    f();

    expect(g.removeClass).toBeCalledWith('active');
    expect(g.find).toBeCalledWith('> .dropdown-menu > .active');
    expect(g.end).toBeCalledTimes(1);
    expect(g.attr).toBeCalledWith('data-toggle', 'tab');
    expect(b.addClass).toBeCalledWith('active');
    expect(b.find).toBeCalledWith('[data-toggle="tab"]');
    expect(b.attr).toBeCalledWith('aria-expanded', true);
    expect(b.removeClass).not.toBeCalled();
    expect(e).toBeCalledTimes(1);
  });

  test('f function without h', () => {
    window.g = g;
    window.b = b;
    window.h = false;
    window.e = e;

    f();

    expect(g.removeClass).toBeCalledWith('active');
    expect(g.find).toBeCalledWith('> .dropdown-menu > .active');
    expect(g.end).toBeCalledTimes(1);
    expect(g.attr).toBeCalledWith('data-toggle', 'tab');
    expect(b.addClass).toBeCalledWith('active');
    expect(b.find).toBeCalledWith('[data-toggle="tab"]');
    expect(b.attr).toBeCalledWith('aria-expanded', true);
    expect(b.removeClass).toBeCalledWith('fade');
    expect(e).toBeCalledTimes(1);
  });

});
