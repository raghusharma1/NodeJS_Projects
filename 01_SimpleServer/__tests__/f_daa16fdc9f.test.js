// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=f_14aa59bc21
ROOST_METHOD_SIG_HASH=f_daa16fdc9f


*/

// ********RoostGPT********
// Import the necessary dependencies
const bootstrap = require('../js/bootstrap.min.js');

// Test suite for the 'f' function
describe('Function f', () => {
    let b, g, e;
    
    // Set up the necessary preconditions before each test
    beforeEach(() => {
        b = {
            addClass: jest.fn(),
            removeClass: jest.fn(),
            find: jest.fn(),
            attr: jest.fn(),
            parent: jest.fn(),
            closest: jest.fn(),
        };
        
        g = {
            removeClass: jest.fn(),
            find: jest.fn(),
        };
        
        e = jest.fn();
    });
    
    // Test case for the 'f' function when 'h' is true
    test('should handle the case when h is true', () => {
        const h = true;
        bootstrap.f(b, g, e, h);
        
        expect(b.addClass).toHaveBeenCalledTimes(2);
        expect(b.removeClass).not.toHaveBeenCalled();
        expect(g.removeClass).toHaveBeenCalled();
        expect(e).toHaveBeenCalled();
    });
    
    // Test case for the 'f' function when 'h' is false
    test('should handle the case when h is false', () => {
        const h = false;
        bootstrap.f(b, g, e, h);
        
        expect(b.addClass).toHaveBeenCalledTimes(1);
        expect(b.removeClass).toHaveBeenCalled();
        expect(g.removeClass).toHaveBeenCalled();
        expect(e).toHaveBeenCalled();
    });
});
