// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=b_fb94df674e
ROOST_METHOD_SIG_HASH=b_15079fdf29


*/

// ********RoostGPT********
const b = require('../js/bootstrap.min');

describe('Bootstrap minified function test', () => {
    let a;

    beforeEach(() => {
        a = jest.fn();
    });

    test('Input of type object', () => {
        const input = { key: 'value' };
        a.mockReturnValue({
            each: jest.fn().mockImplementation((fn) => {
                fn();
            }),
            data: jest.fn().mockReturnValue({
                bs: {
                    affix: 'testAffix'
                }
            })
        });

        const result = b(input);
        expect(a).toHaveBeenCalled();
        expect(result).toBeDefined();
        expect(result.each).toHaveBeenCalled();
    });

    test('Input of type string', () => {
        const input = 'testString';
        a.mockReturnValue({
            each: jest.fn().mockImplementation((fn) => {
                fn();
            }),
            data: jest.fn().mockReturnValue({
                bs: {
                    affix: jest.fn()
                }
            })
        });

        const result = b(input);
        expect(a).toHaveBeenCalled();
        expect(result).toBeDefined();
        expect(result.each).toHaveBeenCalled();
        expect(result.data().bs.affix).toHaveBeenCalled();
    });

    test('Input is neither object nor string', () => {
        const input = 1234;
        a.mockReturnValue({
            each: jest.fn().mockImplementation((fn) => {
                fn();
            }),
            data: jest.fn().mockReturnValue({
                bs: {
                    affix: 'testAffix'
                }
            })
        });

        const result = b(input);
        expect(a).toHaveBeenCalled();
        expect(result).toBeDefined();
        expect(result.each).toHaveBeenCalled();
    });
});
