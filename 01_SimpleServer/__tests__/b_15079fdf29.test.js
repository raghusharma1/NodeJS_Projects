// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=b_fb94df674e
ROOST_METHOD_SIG_HASH=b_15079fdf29


*/

// ********RoostGPT********
const b = require('../js/bootstrap.min');

describe('Bootstrap function b', () => {
  let a, d, e, f;
  
  beforeEach(() => {
    // Mock the dependencies
    a = jest.fn();
    d = { data: jest.fn() };
    e = { new: jest.fn() };
    f = { b: jest.fn() };
    
    // Mock the 'this' context
    globalThis.each = jest.fn((callback) => callback());
    globalThis.string = jest.fn();
  });

  afterEach(() => {
    jest.clearAllMocks();
  });

  test('should call each function of "this" context', () => {
    b('test');
    expect(globalThis.each).toHaveBeenCalled();
  });

  test('should call a function with "this" context', () => {
    b('test');
    expect(a).toHaveBeenCalledWith(globalThis);
  });

  test('should call data function of d object', () => {
    b('test');
    expect(d.data).toHaveBeenCalledWith('bs.affix');
  });

  test('should call new function of e object', () => {
    b('test');
    expect(e.new).toHaveBeenCalledWith(globalThis, 'test');
  });

  test('should call b function of f object if b is a string', () => {
    b('test');
    expect(f.b).toHaveBeenCalled();
  });

  test('should not call b function of f object if b is not a string', () => {
    b(123);
    expect(f.b).not.toHaveBeenCalled();
  });
});
