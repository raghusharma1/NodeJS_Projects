// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=b_fb94df674e
ROOST_METHOD_SIG_HASH=b_15079fdf29


*/

// ********RoostGPT********
const b = require('../js/bootstrap.min');

describe('Bootstrap function b', () => {
  let a, d, e, f;
  
  beforeEach(() => {
    a = jest.fn();
    d = {
      data: jest.fn()
    };
    e = {
      new: jest.fn(),
      b: jest.fn()
    };
    f = "test";
  });

  afterEach(() => {
    jest.clearAllMocks();
  });

  test('should call each function correctly', () => {
    const eachFn = jest.fn((fn) => fn());
    const thisObj = {
      each: eachFn
    };

    b.call(thisObj, f);

    expect(eachFn).toHaveBeenCalledTimes(1);
  });

  test('should call data function correctly', () => {
    const eachFn = jest.fn((fn) => fn());
    const thisObj = {
      each: eachFn
    };

    a.mockReturnValue(d);
    d.data.mockReturnValue(e);

    b.call(thisObj, f);

    expect(a).toHaveBeenCalledTimes(1);
    expect(d.data).toHaveBeenCalledWith("bs.affix");
  });

  test('should create new e object if e is not defined', () => {
    const eachFn = jest.fn((fn) => fn());
    const thisObj = {
      each: eachFn
    };

    a.mockReturnValue(d);
    d.data.mockReturnValue(undefined);
    e.new.mockReturnValue(e);

    b.call(thisObj, f);

    expect(e.new).toHaveBeenCalledWith(thisObj, f);
  });

  test('should call e.b function if b is a string', () => {
    const eachFn = jest.fn((fn) => fn());
    const thisObj = {
      each: eachFn
    };

    a.mockReturnValue(d);
    d.data.mockReturnValue(e);
    e.b.mockReturnValue(undefined);
    f = "string";

    b.call(thisObj, f);

    expect(e.b).toHaveBeenCalledTimes(1);
  });
});
