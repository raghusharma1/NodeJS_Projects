// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=d_39bb39108d
ROOST_METHOD_SIG_HASH=d_72e6da76f1


*/

// ********RoostGPT********
// Importing necessary modules for testing
const bootstrap = require("../js/bootstrap.min");

// Test Suite for function d
describe("Function d Test Suite", () => {
    let f, e, b;

    // Setup before each test
    beforeEach(() => {
        f = {
            detach: jest.fn(),
        };
        e = {
            hoverState: "out",
            $element: {
                removeAttr: jest.fn(),
                trigger: jest.fn(),
            },
            type: "bs",
        };
        b = jest.fn();
    });

    // Test case 1: Check if function f.detach gets called
    test("Should call f.detach function", () => {
        bootstrap.d();
        expect(f.detach).toHaveBeenCalled();
    });

    // Test case 2: Check if removeAttr and trigger methods get called on e.$element
    test("Should call removeAttr and trigger methods on e.$element", () => {
        bootstrap.d();
        expect(e.$element.removeAttr).toHaveBeenCalledWith("aria-describedby");
        expect(e.$element.trigger).toHaveBeenCalledWith("hidden.bs." + e.type);
    });

    // Test case 3: Check if b gets called when e.hoverState is not 'in'
    test("Should call b when e.hoverState is not 'in'", () => {
        bootstrap.d();
        expect(b).toHaveBeenCalled();
    });

    // Test case 4: Check if b does not get called when e.hoverState is 'in'
    test("Should not call b when e.hoverState is 'in'", () => {
        e.hoverState = "in";
        bootstrap.d();
        expect(b).not.toHaveBeenCalled();
    });
});
