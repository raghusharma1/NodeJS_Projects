// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=d_39bb39108d
ROOST_METHOD_SIG_HASH=d_72e6da76f1


*/

// ********RoostGPT********
const bootstrap = require('../js/bootstrap.min.js');

describe("Bootstrap Component Test Suite", () => {
    let hoverState;
    let detach;
    let removeAttr;
    let trigger;
    let b;

    beforeEach(() => {
        hoverState = "out";
        detach = jest.fn();
        removeAttr = jest.fn();
        trigger = jest.fn();
        b = jest.fn();
    });

    test("Should correctly detach, remove attribute and trigger event when hover state is not 'in'", () => {
        bootstrap.d(hoverState, {detach}, {$element: {removeAttr, trigger}}, b);

        expect(detach).toHaveBeenCalled();
        expect(removeAttr).toHaveBeenCalledWith('aria-describedby');
        expect(trigger).toHaveBeenCalledWith('hidden.bs.');
        expect(b).toHaveBeenCalled();
    });

    test("Should not detach, remove attribute and trigger event when hover state is 'in'", () => {
        hoverState = "in";

        bootstrap.d(hoverState, {detach}, {$element: {removeAttr, trigger}}, b);

        expect(detach).not.toHaveBeenCalled();
        expect(removeAttr).not.toHaveBeenCalled();
        expect(trigger).not.toHaveBeenCalled();
        expect(b).toHaveBeenCalled();
    });
});
