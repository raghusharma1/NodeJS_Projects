// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=d_39bb39108d
ROOST_METHOD_SIG_HASH=d_72e6da76f1


*/

// ********RoostGPT********
// Importing necessary modules for testing
const bootstrap = require("../js/bootstrap.min.js");

// Test Suite for function d
describe("Function d tests", () => {
    let f, b, e;

    // Setup necessary variables and functions before each test
    beforeEach(() => {
        f = {
            detach: jest.fn()
        };
        b = jest.fn();
        e = {
            hoverState: 'out',
            $element: {
                removeAttr: jest.fn(),
                trigger: jest.fn()
            },
            type: 'test'
        };
    });

    // Test Case 1: Check if function calls the necessary methods when hoverState is not 'in'
    test("Check if function calls the necessary methods when hoverState is not 'in'", () => {
        bootstrap.d();
        expect(f.detach).toHaveBeenCalled();
        expect(e.$element.removeAttr).toHaveBeenCalledWith('aria-describedby');
        expect(e.$element.trigger).toHaveBeenCalledWith('hidden.bs.' + e.type);
        expect(b).toHaveBeenCalled();
    });

    // Test Case 2: Check if function does not call the necessary methods when hoverState is 'in'
    test("Check if function does not call the necessary methods when hoverState is 'in'", () => {
        e.hoverState = 'in';
        bootstrap.d();
        expect(f.detach).not.toHaveBeenCalled();
        expect(e.$element.removeAttr).not.toHaveBeenCalled();
        expect(e.$element.trigger).not.toHaveBeenCalled();
        expect(b).not.toHaveBeenCalled();
    });
});
