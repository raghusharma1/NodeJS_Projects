// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=d_39bb39108d
ROOST_METHOD_SIG_HASH=d_72e6da76f1


*/

// ********RoostGPT********
const bootstrap = require('../js/bootstrap.min.js');

describe("Bootstrap Component Test Suite", () => {
  let hoverState;
  let element;
  let type;
  let b;

  beforeEach(() => {
    hoverState = 'out';
    element = {
      removeAttr: jest.fn(),
      trigger: jest.fn()
    };
    type = 'bs';
    b = jest.fn();
  });

  test("should call removeAttr and trigger when hoverState is not 'in'", () => {
    bootstrap.d(hoverState, element, type, b);
    expect(element.removeAttr).toHaveBeenCalledWith('aria-describedby');
    expect(element.trigger).toHaveBeenCalledWith('hidden.bs.' + type);
    expect(b).not.toHaveBeenCalled();
  });

  test("should call removeAttr, trigger and b when hoverState is 'in'", () => {
    hoverState = 'in';
    bootstrap.d(hoverState, element, type, b);
    expect(element.removeAttr).toHaveBeenCalledWith('aria-describedby');
    expect(element.trigger).toHaveBeenCalledWith('hidden.bs.' + type);
    expect(b).toHaveBeenCalled();
  });
});
