// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=d_39bb39108d
ROOST_METHOD_SIG_HASH=d_72e6da76f1


*/

// ********RoostGPT********
// Importing necessary modules for testing
const bootstrap = require("../js/bootstrap.min.js");

// Test Suite for function d
describe("Function d tests", () => {
    let f, e, b;

    // Setup necessary preconditions
    beforeEach(() => {
        f = {
            detach: jest.fn()
        };
        e = {
            hoverState: "out",
            $element: {
                removeAttr: jest.fn(),
                trigger: jest.fn()
            },
            type: "bs"
        };
        b = jest.fn();
    });

    // Test case 1: Check if f.detach is called when hoverState is not "in"
    test("f.detach is called when hoverState is not 'in'", () => {
        bootstrap.d();
        expect(f.detach).toHaveBeenCalled();
    });

    // Test case 2: Check if removeAttr and trigger are called on $element
    test("$element.removeAttr and $element.trigger are called", () => {
        bootstrap.d();
        expect(e.$element.removeAttr).toHaveBeenCalledWith("aria-describedby");
        expect(e.$element.trigger).toHaveBeenCalledWith("hidden.bs."+e.type);
    });

    // Test case 3: Check if b is called
    test("b is called", () => {
        bootstrap.d();
        expect(b).toHaveBeenCalled();
    });

    // Test case 4: Check if f.detach is not called when hoverState is "in"
    test("f.detach is not called when hoverState is 'in'", () => {
        e.hoverState = "in";
        bootstrap.d();
        expect(f.detach).not.toHaveBeenCalled();
    });
});
