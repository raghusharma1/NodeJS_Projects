// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=d_39bb39108d
ROOST_METHOD_SIG_HASH=d_72e6da76f1


*/

// ********RoostGPT********
const d = require('../js/bootstrap.min.js');

describe('d function', () => {
  let e, f, b;
  
  beforeEach(() => {
    e = {
      hoverState: 'out',
      type: 'test',
      $element: {
        removeAttr: jest.fn().mockReturnThis(),
        trigger: jest.fn()
      }
    };
    f = {
      detach: jest.fn()
    };
    b = jest.fn();
  });

  test('should call f.detach when hoverState is not "in"', () => {
    d();
    expect(f.detach).toHaveBeenCalled();
  });

  test('should not call f.detach when hoverState is "in"', () => {
    e.hoverState = 'in';
    d();
    expect(f.detach).not.toHaveBeenCalled();
  });

  test('should call removeAttr and trigger on $element', () => {
    d();
    expect(e.$element.removeAttr).toHaveBeenCalledWith('aria-describedby');
    expect(e.$element.trigger).toHaveBeenCalledWith('hidden.bs.test');
  });

  test('should call b when b is defined', () => {
    d();
    expect(b).toHaveBeenCalled();
  });

  test('should not call b when b is not defined', () => {
    b = undefined;
    d();
    expect(b).not.toHaveBeenCalled();
  });
});
