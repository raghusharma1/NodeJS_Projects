// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=d_39bb39108d
ROOST_METHOD_SIG_HASH=d_72e6da76f1


*/

// ********RoostGPT********
const bootstrap = require('../js/bootstrap.min.js');

describe("Bootstrap Component Test Suite", () => {
  let hoverState;
  let detach;
  let removeAttr;
  let trigger;
  let b;

  beforeEach(() => {
    hoverState = 'out';
    detach = jest.fn();
    removeAttr = jest.fn();
    trigger = jest.fn();
    b = jest.fn();
  });

  test("Should correctly detach, remove attribute, trigger event and call b", () => {
    const e = {
      hoverState,
      $element: {
        removeAttr,
        trigger
      },
      type: 'type'
    };
    const f = {
      detach
    };
    bootstrap.d.call({ $element: e.$element, type: e.type, f, b });

    expect(e.$element.removeAttr).toHaveBeenCalledWith('aria-describedby');
    expect(e.$element.trigger).toHaveBeenCalledWith('hidden.bs.' + e.type);
    expect(f.detach).toHaveBeenCalled();
    expect(b).toHaveBeenCalled();
  });

  test("Should not detach, remove attribute, trigger event and call b when hoverState is 'in'", () => {
    hoverState = 'in';
    const e = {
      hoverState,
      $element: {
        removeAttr,
        trigger
      },
      type: 'type'
    };
    const f = {
      detach
    };
    bootstrap.d.call({ $element: e.$element, type: e.type, f, b });

    expect(e.$element.removeAttr).not.toHaveBeenCalled();
    expect(e.$element.trigger).not.toHaveBeenCalled();
    expect(f.detach).not.toHaveBeenCalled();
    expect(b).not.toHaveBeenCalled();
  });
});
