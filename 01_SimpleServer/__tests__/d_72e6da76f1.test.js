// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=d_39bb39108d
ROOST_METHOD_SIG_HASH=d_72e6da76f1


*/

// ********RoostGPT********
const bootstrap = require('../js/bootstrap.min.js');

describe("Bootstrap Component Test Suite", () => {
  let hoverState;
  let detach;
  let removeAttr;
  let trigger;
  let b;

  beforeEach(() => {
    hoverState = 'out';
    detach = jest.fn();
    removeAttr = jest.fn();
    trigger = jest.fn();
    b = jest.fn();
  });

  afterEach(() => {
    hoverState = null;
    detach = null;
    removeAttr = null;
    trigger = null;
    b = null;
  });

  test("Test d() function with hoverState not equals to 'in'", () => {
    bootstrap.d();
    expect(detach).toHaveBeenCalled();
    expect(removeAttr).toHaveBeenCalledWith('aria-describedby');
    expect(trigger).toHaveBeenCalledWith('hidden.bs.' + bootstrap.e.type);
    expect(b).toHaveBeenCalled();
  });

  test("Test d() function with hoverState equals to 'in'", () => {
    hoverState = 'in';
    bootstrap.d();
    expect(detach).not.toHaveBeenCalled();
    expect(removeAttr).toHaveBeenCalledWith('aria-describedby');
    expect(trigger).toHaveBeenCalledWith('hidden.bs.' + bootstrap.e.type);
    expect(b).toHaveBeenCalled();
  });
});
