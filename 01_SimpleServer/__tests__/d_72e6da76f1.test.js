// ********RoostGPT********
/*
Test generated by RoostGPT for test sampleNodeJSEnv using AI Type  and AI Model 

ROOST_METHOD_HASH=d_39bb39108d
ROOST_METHOD_SIG_HASH=d_72e6da76f1


*/

// ********RoostGPT********
const bootstrap = require('../js/bootstrap.min.js');

describe("Bootstrap Component Test Suite", () => {
  let hoverState;
  let element;
  let type;
  let height;

  beforeEach(() => {
    hoverState = 'out';
    type = 'test';
    height = 20;

    element = {
      removeAttr: jest.fn(),
      trigger: jest.fn()
    };

    global.$element = element;
    global.f = {
      detach: jest.fn()
    };
  });

  afterEach(() => {
    hoverState = null;
    type = null;
    height = null;
    element = null;
  });

  test("Check if hoverState is not 'in' and f.detach and $element.removeAttr are called", () => {
    bootstrap.d();
    expect(f.detach).toHaveBeenCalled();
    expect(element.removeAttr).toHaveBeenCalledWith('aria-describedby');
    expect(element.trigger).toHaveBeenCalledWith('hidden.bs.' + type);
  });

  test("Check if hoverState is 'in' and f.detach and $element.removeAttr are not called", () => {
    hoverState = 'in';
    bootstrap.d();
    expect(f.detach).not.toHaveBeenCalled();
    expect(element.removeAttr).not.toHaveBeenCalled();
    expect(element.trigger).not.toHaveBeenCalled();
  });
});
